/**
 * DESIGN TOKENS — TypeScript entry point
 *
 * Usage:
 * import { colors, spacing } from '@repo/design-system/tokens'
 */

export {
  colors,
  spacing,
  radius,
  fontSize,
  fontFamily,
  transition,
} from './primitives';